describe('the mouseMove object', function() {

    //Create an easily-removed container for our tests to play in
    beforeEach(function() {
    });

    //Clean it up after each spec
    afterEach(function() {
    });

    //Specs
    describe('click tests', function() {

        it('updates mouse coordinates', function() {   
//            expect(mouseMove.update()).toBe(0);
        });

        it('enables mouse movement', function() {
//            expect(mouseMove.enable()).toBe(0);
        });

        it('disables mouse movement', function() {
//            expect(mouseMove.disable()).toBe(0);
        });

    });

});